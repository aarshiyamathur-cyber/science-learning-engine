# Prompts

Versioned prompt templates used by the learning engine and workers (e.g. hint generation, misconception detection, content drafting) when calling an LLM — local via [Ollama](../workers/ollama-client) or otherwise.

Keep prompts here as data, not inlined in application code, so they can be reviewed, versioned, and evaluated independently of the code that calls them.
